# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs — intentionally only top-level (bare `logs` would swallow
# app/pages/logs/ in a Nuxt app and silently exclude source files).
/logs/
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
