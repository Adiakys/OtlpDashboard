{
  "name": "Database size",
  "description": "On-disk size of a Postgres database. Bind to 'postgresql.db_size' (one instrument per DB once the collector splits by 'postgresql.database.name').",
  "icon": "i-ph-hard-drives",
  "defaultSize": { "w": 3, "h": 3 },
  "engine": "preset",
  "baseKind": "metric-stat",
  "defaultConfig": {
    "metric": null,
    "range": "last-15m",
    "showSparkline": true,
    "decimals": 1,
    "unitKind": "bytes",
    "calc": "last"
  }
}
